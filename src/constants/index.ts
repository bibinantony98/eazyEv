export enum StoredKeys  {
	USER_DETAILS =  "USER_DETAILS"
}

export enum Language {
    ENGLISH = "en",
    RUSSIAN = "ru"
}

export enum ResponseType {
    FULFILLED = "FULFILLED",
    REJECTED = "REJECTED"
}